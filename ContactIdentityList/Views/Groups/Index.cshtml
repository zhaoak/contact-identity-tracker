@{
  Layout = "_Layout";
}

<h2>Full Groups List</h2>

@using ContactIdentityList.Models;

@if (Model.Count == 0)
{
  <p><em>No groups.</em></p>
}
else
{
  <ul>
  @foreach (Group group in Model)
  {
    <li>@Html.ActionLink("@group.Name", "Details", "Identities")</li>
  }
  </ul>
}
